# Decisions

| Date | Decision | Context | Options Considered | Why | Consequences | Revisit Trigger |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-05-12 | Create repo-local workflow lane `swe-af-adoption` | The initial adoption work was done directly in the plugin repo and smoke-tested with temporary `.workflow` folders under `/private/tmp`. | Copy temp `demo` fixtures; create a clean repo-local lane; do nothing. | The temp workflows are synthetic validation fixtures, not the real adoption state. | `.workflow/swe-af-adoption/` is now the tracking location for ongoing adoption work. | Revisit if a different lane slug or multi-lane structure is needed. |
| 2026-05-12 | Archive selected temp workflows as fixture evidence | `/private/tmp` contains smoke-test lanes for DAG, lane blocks, and parallel dispatch. | Move selected temp workflows into the repo; keep only references; discard them silently. | Keeping exact fixture state is useful for later comparison, as long as it is not confused with the canonical adoption lane. | Selected fixtures live under `tests/fixtures/workflows/wrkflw-*`; `.workflow/swe-af-adoption/` remains the canonical adoption lane. | Revisit if smoke fixtures should move under a different test fixture layout. |
| 2026-05-12 | Implement typed debt before checkpoint/resume | The next increment needed to build on the DAG without touching command transaction internals too broadly. | Checkpoint/resume; typed debt propagation; git worktree isolation. | Typed debt fits the current artifact model and immediately improves planning, review, and downstream story context. | `records/debt.jsonl`, `debt.md`, `wrkflw:debt-record`, DAG debt propagation, and release debt blocking are now part of the adoption path. | Revisit once issue advisor or replanner needs richer debt lifecycle states. |
